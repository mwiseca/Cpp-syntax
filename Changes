Cpp syntax was built entirly with C-strings code.
The arrays and other code was changed to write c++ code.
march 14 2024.
A Python version has been added. its in the python branch.
March 28 2024.
A C version was added to the C branch.
April 2 2024.
Start a map was added. It starts a map with one key value pair.
Check mp in choices.
Check else if(strcmp(sw, "mp")==0){ to see code.
April 15 2024.
In if statements for numbers choose a operator was changed to not start over
if a wrong letter was chosen.
Check in in choices.
Check }else if(strcmp(sw, "in")==0){ to see code.
April 15 2024.
The cy selection was updated for maps. #include <map>, try{,and }catch(std::out_of_range){
was added.
Check cy in choices.
April 16 2024.
Map access was added.
check ma in choices.
Check }else if(strcmp(sw, "ma")==0){ to see code.
April 16 2024.
